spring:
    datasource:
        jndi-name: jdbc/simpleaos
    thymeleaf:
        cache: false
        mode: HTML
    jpa:
        hibernate:
            naming:
                physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    mvc:
        static-path-pattern: /resources/**
    messages:
        encoding: UTF-8 
flyway:
    baseline-version: 1
    check-location: false
    enabled: true
    locations: classpath:db/migrations
    out-of-order: true
